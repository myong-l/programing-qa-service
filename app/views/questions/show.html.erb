<p id="notice"><%= notice %></p>
<div class="jumbotron">
    <h1><%= @question.title %></h1>
    <p><%= @question.body %></p>
  <small><%= @question.user.name %> | <%= @question.created_at %></small>
    <p><%= link_to 'Edit', edit_question_path(@question), class: 'btn btn-primary' %></p>
</div>
